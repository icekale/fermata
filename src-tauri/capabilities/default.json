{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Fermata's renderer surfaces: settings window, tray popover, break windows.",
  "windows": ["*"],
  "permissions": ["core:default", "core:window:allow-close"]
}
